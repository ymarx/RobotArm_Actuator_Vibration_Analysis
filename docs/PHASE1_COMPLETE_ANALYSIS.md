# Phase 1 완료 분석 보고서

**작성일**: 2025-11-17
**분석 범위**: 데이터 전처리 검증 및 탐색적 분석
**다음 단계**: Phase 2 (XGBoost Baseline 모델링) 준비

---

## 📊 Executive Summary

### ✅ 성공적인 개선사항
- **품질 기준 조정**: 제품별 측정 시간 차이 반영 → 사용 가능 파일 51.7% → **95.0%** 향상
- **데이터 누수 검증**: 5가지 검증 테스트 모두 통과 → 데이터 분할 무결성 확인
- **Feature 추출**: 49개 시간 영역 feature 성공적 생성
- **클래스 분리 가능성**: 28/49 features가 통계적으로 유의미한 차이 (p<0.05)

### ⚠️ 주요 이슈
1. **불균형한 데이터셋**: 정상 13개 vs 불량 47개 파일 (1:3.6 비율)
2. **결측치**: 5개 윈도우에서 49개 feature 모두 NaN (200W_S02_CW_R4)
3. **낮은 Effect Size**: Large effect (|d|>0.8) feature 0개 → 클래스 분리 어려움 예상
4. **고상관 Features**: 31개 feature 쌍이 |r|>0.9 → 중복성 높음

---

## 1️⃣ 데이터 전처리 검증 결과

### 1.1 품질 검사 통과율

| 항목 | 결과 | 비고 |
|-----|------|------|
| **총 파일** | 60개 | 100W: 30개, 200W: 30개 |
| **사용 가능** | 57개 (95.0%) | ✅ 목표 달성 |
| **품질 실패** | 3개 (5.0%) | 200W_S02 3개 파일 |

**제품별 통과율**:
- **100W**: 30/30 (100%) ✅
- **200W**: 27/30 (90.0%) ⚠️

**품질 실패 파일**:
```
200W_S02_CCW_R1: 43,689 samples (expected: 32k-36k) - 21% 초과
200W_S02_CW_R1:  59,462 samples (expected: 32k-36k) - 65% 초과
200W_S02_CW_R4:  53,994 samples (expected: 32k-36k) - 50% 초과
```

### 1.2 라벨 분포

#### 원본 라벨 (Raw)
```
표기없음: 38개 (63.3%)
소음:      9개 (15.0%)
진동:      9개 (15.0%)
정상:      4개 (6.7%)  ← 양품 파일 매우 부족
```

#### 이진 분류 라벨
```
불량 (0): 47개 파일 (78.3%)
정상 (1): 13개 파일 (21.7%)  ← 200W_S03의 소음 9개 → 양품 재분류

정상:불량 비율 = 1:3.6
```

### 1.3 데이터 분할 검증 ✅

**5가지 데이터 누수 검증 테스트 모두 통과**:

1. ✅ **파일 레벨 누수 없음**: 동일 파일의 윈도우가 여러 split에 등장하지 않음 (time_split 제외)
2. ✅ **시간 분할 경계 준수**: time_split 윈도우가 지정된 시간 구간 내에 위치
3. ✅ **방향 일관성**: 동일 파일 내 CW/CCW 방향 일관됨
4. ✅ **Val/Test 독립성**: Balancing 후에도 val/test 세트 변경 없음
5. ✅ **시간 중복 없음**: Split 간 윈도우 시간 중복 없음

### 1.4 윈도우 분할 결과

| Split | 정상 | 불량 | 총 윈도우 | 비율 |
|-------|------|------|----------|------|
| **Train** | 160 (33.3%) | 321 (66.7%) | 481 | 1:2.0 (balancing 적용) |
| **Val** | 4 (4.3%) | 88 (95.7%) | 92 | 1:22.0 ⚠️ |
| **Test** | 14 (12.6%) | 97 (87.4%) | 111 | 1:6.9 ⚠️ |
| **Total** | 178 | 506 | 684 | 1:2.8 |

**⚠️ Val/Test 불균형 이슈**:
- Validation set에 정상 윈도우 4개만 존재 → 신뢰성 있는 검증 어려움
- Test set도 정상 14개로 부족 → 일반화 평가 제한적

---

## 2️⃣ Feature 탐색적 분석 결과

### 2.1 Feature 구성

**총 62개 컬럼**:
- **센서 Feature**: 49개 (acc-X/Y/Z/Sum + Gyro-X/Y/Z)
- **메타 Feature**: 11개 (window_id, file_id, split_set, product, sample, direction, labels 등)
- **윈도우 정보**: 2개 (window_duration, window_num_samples)

**채널별 Feature**:
- Acceleration (X/Y/Z/Sum): 7개 통계량 × 4채널 = 28개
- Gyroscope (X/Y/Z): 7개 통계량 × 3채널 = 21개

**통계량**: mean, std, rms, peak, crest_factor, kurtosis, skewness

### 2.2 클래스 분리 가능성 분석 (Train Set)

#### 통계적 유의성
- **Significant features** (p<0.05): **28/49** (57.1%)
- **Large effect size** (|d|>0.8): **0/49** (0%) ⚠️
- **Medium effect size** (0.5≤|d|<0.8): **2/49** (4.1%)

#### Top 10 구별력 높은 Features (Cohen's d 기준)

| Rank | Feature | Cohen's d | p-value | 해석 |
|------|---------|-----------|---------|------|
| 1 | **Gyro_Y_std** | -0.599 | 7.08e-12 | 불량품에서 Y축 회전 변동 큼 |
| 2 | **acc_Y_skewness** | -0.612 | 1.36e-09 | 불량품에서 Y축 가속도 비대칭 |
| 3 | **acc_Y_rms** | -0.477 | 4.78e-09 | 불량품에서 Y축 진동 크기 큼 |
| 4 | **acc_Y_std** | -0.472 | 6.61e-09 | 불량품에서 Y축 진동 변동 큼 |
| 5 | **acc_Sum_mean** | -0.474 | 7.61e-09 | 불량품에서 전체 가속도 평균 높음 |
| 6 | **Gyro_X_std** | -0.535 | 8.78e-09 | 불량품에서 X축 회전 변동 큼 |
| 7 | **acc_Sum_rms** | -0.469 | 1.01e-08 | 불량품에서 전체 진동 크기 큼 |
| 8 | **acc_X_peak** | -0.472 | 1.10e-08 | 불량품에서 X축 최대 진폭 큼 |
| 9 | **acc_Sum_peak** | -0.468 | 1.22e-08 | 불량품에서 전체 최대 진폭 큼 |
| 10 | **acc_X_rms** | -0.470 | 1.37e-08 | 불량품에서 X축 진동 크기 큼 |

**패턴 분석**:
- 🔴 **모든 Cohen's d가 음수** → 불량품이 정상품보다 진동/회전 크기가 큼
- 📊 **Y축 관련 feature가 상위권** → Y축 방향 진동이 불량 감지에 중요
- 🔄 **Gyroscope feature도 상위** → 회전 운동 패턴이 불량 특성 반영
- ⚠️ **Effect size가 모두 small-medium** → 단일 feature로 완벽한 분류 어려움

### 2.3 Feature 상관관계 분석

#### 고상관 Feature 쌍 (|r| > 0.9)
- **총 31개 쌍 발견**
- 주로 **동일 채널 내 std ↔ rms ↔ peak** 간 강한 상관

**예시**:
```
acc_X_std ↔ acc_X_rms:  r = 0.9999  ← 거의 중복
acc_Y_std ↔ acc_Y_rms:  r = 0.9997
acc_X_std ↔ acc_X_peak: r = 0.9698
acc_X_std ↔ acc_Z_std:  r = 0.9537  ← 축 간 상관
```

**함의**:
- 📉 **Feature 중복성 높음** → 차원 축소 필요 (PCA, feature selection)
- 🎯 **std, rms, peak 중 하나만 선택 가능** → 모델 복잡도 감소
- ⚡ **다중공선성 이슈** → Linear model보다 Tree-based model 유리

### 2.4 이상치 (Outlier) 분석

**이상치가 있는 Features**: 40/49 (81.6%)

**Top 5 Outlier Features**:
| Feature | Outlier 비율 | 해석 |
|---------|-------------|------|
| Gyro_Z_kurtosis | 10.1% | Z축 회전 첨도 극값 많음 |
| acc_Sum_std | 7.8% | 전체 진동 변동 극값 |
| acc_Y_std | 7.8% | Y축 진동 변동 극값 |
| acc_Y_rms | 7.8% | Y축 진동 크기 극값 |
| acc_Y_peak | 7.1% | Y축 최대 진폭 극값 |

**이상치 처리 방안**:
- ✅ **XGBoost는 outlier에 robust** → 제거하지 않고 그대로 학습
- 📊 **이상치가 불량품 특징일 가능성** → 제거하면 정보 손실 우려
- 🔍 **모델 학습 후 feature importance로 검증** 필요

### 2.5 결측치 (Missing Values)

**결측 윈도우**: 5개 (전체의 0.7%)
- **파일**: 200W_S02_CW_R4
- **영향**: 49개 feature 모두 NaN

**처리 방안**:
1. **제거 (권장)**: 0.7%로 영향 미미, 단순 제거
2. **Imputation**: Mean/Median 대체 (비권장 - 정보 왜곡)

---

## 3️⃣ 제품/방향별 특성 비교

### 3.1 제품별 비교 (100W vs 200W)

**파일 분포**:
- 100W: 30개 파일 (모두 사용 가능)
- 200W: 30개 파일 (27개 사용 가능, 3개 품질 실패)

**측정 시간 차이**:
- 100W: ~27k-29k samples (~53-57초)
- 200W: ~32k-36k samples (~62-68초) ← **더 긴 측정**

**Feature 차이 분석** 필요:
- 제품 출력(100W vs 200W)에 따른 진동 패턴 차이
- 모델 학습 시 `product` feature 포함 여부 검토

### 3.2 방향별 비교 (CW vs CCW)

**파일 분포**:
- CW (시계방향): 30개
- CCW (반시계방향): 30개

**Feature 차이 분석** 필요:
- 회전 방향에 따른 진동/회전 패턴 차이
- Gyroscope sign 반전 여부 확인

---

## 4️⃣ Phase 2 준비사항 및 권장 사항

### 4.1 데이터 전처리 최종 조치

#### ✅ 즉시 조치 필요
1. **결측치 처리**:
   ```python
   # 5개 윈도우 제거 (0.7% 영향 미미)
   features_clean = features.dropna()
   ```

2. **품질 실패 파일 처리**:
   - 현재: 자동 제외됨 (file_master에서 quality_pass=False)
   - 확인: 200W_S02의 3개 파일이 모델링에서 제외되었는지 검증

#### 🔍 검토 사항
3. **Feature 중복성 해결**:
   - **Option A**: 고상관 쌍 중 하나 제거 (수동 선택)
   - **Option B**: XGBoost feature_importance 기반 선택
   - **Option C**: PCA로 차원 축소
   - **권장**: Option B (모델 기반 선택)

4. **Val/Test 불균형 해결**:
   - **현상**: Val에 정상 4개, Test에 정상 14개만 존재
   - **원인**: 양품 파일 부족 (100W_S00, 200W_S03만 존재)
   - **해결책**:
     - **Option A**: Cross-validation (StratifiedKFold) 사용 ← **권장**
     - **Option B**: 시간 분할 비율 재조정 (train 50%, val 20%, test 30%)
     - **Option C**: 데이터 추가 수집 (장기 계획)

### 4.2 Phase 2 모델링 전략

#### Baseline Model: XGBoost

**장점**:
- ✅ Tree-based → 다중공선성에 robust
- ✅ Outlier에 강건
- ✅ Feature importance 제공
- ✅ Class imbalance 처리 가능 (`scale_pos_weight`)

**학습 설정**:
```python
params = {
    'objective': 'binary:logistic',
    'eval_metric': 'auc',
    'scale_pos_weight': 321/160,  # 불량/정상 비율
    'max_depth': 6,
    'learning_rate': 0.01,
    'n_estimators': 1000,
    'early_stopping_rounds': 50
}
```

**평가 지표**:
- **Primary**: AUC-ROC (불균형 데이터에 적합)
- **Secondary**: Precision, Recall, F1-score
- **Threshold 조정**: 불량품 탐지율(Recall) 우선 vs 오탐률(FPR) 최소화

#### Feature Engineering 추가 검토

**현재 부족한 부분**:
1. **주파수 영역 features** ← **가장 중요**
   - FFT peak frequencies
   - Power spectral density
   - Spectral entropy
   - Dominant frequency

2. **고급 시간 영역 features**:
   - Zero-crossing rate
   - Autocorrelation
   - Wavelet coefficients

3. **Cross-channel features**:
   - X-Y-Z 간 상관계수
   - 축 간 phase lag

**우선순위**:
1️⃣ XGBoost Baseline 먼저 학습 (현재 49개 feature)
2️⃣ Feature importance 분석
3️⃣ 주파수 영역 feature 추가 후 성능 비교

### 4.3 검증 전략

#### Cross-Validation
```python
from sklearn.model_selection import StratifiedKFold

# 5-Fold CV (정상 샘플 부족으로 작은 k 사용)
cv = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)

# Train set만 CV 수행
for fold, (train_idx, val_idx) in enumerate(cv.split(X_train, y_train)):
    # 모델 학습 및 평가
```

#### Holdout Test Set
- 현재 test set (111 윈도우) 최종 평가용으로 보존
- **주의**: 정상 14개만 존재 → 정상 분류 성능 불안정 가능성

### 4.4 다음 단계 Action Items

#### Week 1: XGBoost Baseline
- [ ] 결측치 5개 윈도우 제거
- [ ] XGBoost baseline 모델 학습 (5-Fold CV)
- [ ] Feature importance 분석
- [ ] 성능 지표 계산 (AUC, Precision, Recall, F1)
- [ ] Confusion matrix 및 ROC curve 시각화

#### Week 2: Feature Engineering
- [ ] 주파수 영역 feature 추출 (FFT-based)
- [ ] Feature selection (중복 feature 제거)
- [ ] 개선된 모델 재학습 및 성능 비교

#### Week 3: Model Optimization
- [ ] Hyperparameter tuning (Optuna)
- [ ] Threshold optimization (Recall 우선 vs Precision 우선)
- [ ] 최종 모델 선정 및 test set 평가

#### Week 4: Advanced Methods
- [ ] Autoencoder anomaly detection 구현
- [ ] Ensemble methods (XGBoost + Autoencoder)
- [ ] Final report 작성

---

## 5️⃣ 리스크 및 제약사항

### 🔴 Critical Risks
1. **양품 샘플 부족**:
   - 파일: 13개 (100W 1개, 200W 1개×9파일 + 소음 재분류)
   - 영향: Val/Test 신뢰성 낮음, 일반화 성능 검증 어려움
   - 완화: Cross-validation 필수, 추가 데이터 수집 장기 계획

2. **낮은 클래스 분리 능력**:
   - Large effect size feature 0개
   - 영향: 높은 분류 정확도 기대 어려움
   - 완화: 주파수 영역 feature 추가, ensemble 방법

### 🟡 Medium Risks
3. **Feature 중복성**:
   - 31개 고상관 쌍
   - 영향: 모델 복잡도 증가, 해석 어려움
   - 완화: Feature selection, PCA

4. **Val/Test 불균형**:
   - Val 정상 4개, Test 정상 14개
   - 영향: 검증/평가 불안정
   - 완화: Cross-validation 사용

### 🟢 Low Risks
5. **결측치**:
   - 5개 윈도우 (0.7%)
   - 영향: 미미
   - 완화: 단순 제거

6. **Outliers**:
   - 40/49 features에 이상치 존재
   - 영향: XGBoost는 robust
   - 완화: 필요 시 모델 학습 후 검토

---

## 6️⃣ 결론 및 권장사항

### ✅ Phase 1 성과
1. **품질 관리 개선**: 51.7% → 95.0% 사용 가능 파일
2. **데이터 무결성 확보**: 데이터 누수 검증 5개 테스트 모두 통과
3. **Feature 추출 완료**: 49개 시간 영역 feature 생성
4. **분석 준비 완료**: EDA 결과 기반 Phase 2 로드맵 수립

### 🎯 Phase 2 핵심 목표
1. **Baseline 성능 확립**: XGBoost로 AUC > 0.80 달성
2. **Feature Engineering**: 주파수 영역 feature 추가로 성능 개선
3. **검증 전략 수립**: Cross-validation으로 신뢰성 있는 평가

### 📋 즉시 실행 항목
```python
# 1. 결측치 제거
features_clean = features.dropna()

# 2. Train/Val/Test split 확인
print(features_clean.groupby(['split_set', 'label_binary']).size())

# 3. XGBoost baseline 학습 준비
X = features_clean[sensor_features]
y = features_clean['label_binary']
```

### 🚀 Next Steps
**이제 Phase 2로 진행 준비 완료!**

계속 진행할까요? XGBoost baseline 모델 학습을 시작하시겠습니까?
