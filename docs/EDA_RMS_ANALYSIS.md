# RMS Feature 심층 분석 보고서

**작성일**: 2025-11-17
**목적**: RMS feature의 클래스 분리 능력 및 중복성 분석
**데이터**: Train set (476 windows, Normal: 155, Abnormal: 321)

---

## 📊 Executive Summary

### ✅ 주요 발견사항
1. **RMS는 불량 감지의 핵심 지표**: 불량품이 정상품 대비 **1.5~2.2배** 높은 RMS 값
2. **acc_Y_rms가 가장 강력**: 불량품이 정상품 대비 **2.22배** (+122.5%)
3. **고상관 문제**: 5개 RMS 쌍이 r>0.95로 **중복성 높음**
4. **Gyro_Z_rms 예외**: 불량품이 오히려 **낮음** (-5.9%)

### ⚠️ 주요 이슈
- **Feature 중복**: acc_Y_rms ↔ acc_Sum_rms (r=0.994) → 하나만 선택 가능
- **극단적 이상치**: acc_Y_rms 최대값 2.59 (정상 최대 0.23의 11배)
- **분포 중첩**: Median 기준으로 정상/불량 구간 중첩 존재

---

## 1️⃣ RMS Features 목록 및 물리적 의미

### 1.1 RMS Features (7개)

| Feature | 물리적 의미 | 측정 대상 |
|---------|------------|----------|
| **acc_X_rms** | X축 선형 진동 크기 | 좌우 흔들림 |
| **acc_Y_rms** | Y축 선형 진동 크기 | 전후 흔들림 |
| **acc_Z_rms** | Z축 선형 진동 크기 | 상하 흔들림 |
| **acc_Sum_rms** | 합성 진동 크기 | 전체 진동 에너지 |
| **Gyro_X_rms** | X축 회전 흔들림 | Roll 방향 회전 불안정 |
| **Gyro_Y_rms** | Y축 회전 흔들림 | Pitch 방향 회전 불안정 |
| **Gyro_Z_rms** | Z축 회전 흔들림 | Yaw 방향 회전 불안정 |

### 1.2 RMS의 중요성 (문서 참조)

**문서 02_분석목적-데이터현황-배경지식.md에서**:
- **대역 RMS**: 주파수 대역별 에너지 측정 (1-10 Hz, 10-50 Hz, 50-150 Hz)
- **하이브리드 룰**: `(rms_50_150 > T2)` 임계값 기반 불량 판정
- **축 불변 지표**: 센서 정렬 불확실성 보완용 핵심 지표
- **계산 공식**: `RMS = sqrt(mean(x^2))`

---

## 2️⃣ 클래스별 RMS 통계 분석

### 2.1 불량/정상 비율 (Abnormal / Normal Mean)

| Rank | Feature | Normal Mean | Abnormal Mean | Ratio | % Increase |
|------|---------|-------------|---------------|-------|------------|
| 🥇 1 | **acc_Y_rms** | 0.143 | 0.319 | **2.22x** | **+122.5%** |
| 🥈 2 | **acc_Sum_rms** | 0.245 | 0.430 | **1.76x** | **+75.9%** |
| 🥉 3 | **acc_Z_rms** | 0.147 | 0.224 | **1.52x** | **+52.2%** |
| 4 | **acc_X_rms** | 0.130 | 0.192 | **1.48x** | **+48.1%** |
| 5 | **Gyro_Y_rms** | 0.722 | 1.003 | **1.39x** | **+39.0%** |
| 6 | **Gyro_X_rms** | 0.721 | 0.798 | **1.11x** | **+10.6%** |
| 7 | Gyro_Z_rms | 106.3 | 100.0 | 0.94x | -5.9% ⚠️ |

**패턴 분석**:
- 🔴 **Acceleration RMS**: 모두 불량품이 높음 (1.48~2.22배)
- 🟠 **Gyro X/Y**: 불량품이 약간 높음 (1.11~1.39배)
- 🟢 **Gyro Z**: **예외적으로 불량품이 낮음** (-5.9%)

**Gyro_Z_rms 예외 해석**:
- Z축(Yaw) 회전은 액추에이터의 주 회전축
- 불량품이 오히려 회전 안정성이 높을 가능성 (베어링 마찰/간극 영향?)
- 또는 측정 조건/센서 정렬 이슈 가능성
- **Feature 선택 시 제외 또는 역방향 사용 검토 필요**

### 2.2 분포 상세 통계 (Boxplot Summary)

#### acc_Y_rms (가장 강력한 Feature)

| Class | Count | Mean | Median | Q25 | Q75 | Min | Max |
|-------|-------|------|--------|-----|-----|-----|-----|
| **Normal** | 155 | 0.143 | **0.149** | 0.110 | 0.177 | 0.034 | **0.234** |
| **Abnormal** | 321 | 0.319 | **0.154** | 0.129 | 0.192 | 0.064 | **2.587** |

**분석**:
- ✅ **평균 차이 명확**: 불량 0.319 vs 정상 0.143 (2.22배)
- ⚠️ **중앙값 중첩**: 불량 0.154 vs 정상 0.149 (거의 동일)
- 🔴 **극단적 이상치**: 불량 최대값 2.587 (정상 최대 0.234의 **11배**)
- 📊 **분포 특성**: 불량품은 **긴 꼬리 분포** (평균 >> 중앙값)

**함의**:
- 불량품 대부분은 정상과 유사하지만, **일부 극단값**이 평균을 끌어올림
- **임계값 설정**이 중요: Median 기준으로는 분리 어려움
- **이상치 탐지** 접근이 유효할 가능성 (Autoencoder)

#### acc_Sum_rms (합성 진동)

| Class | Count | Mean | Median | Q25 | Q75 | Min | Max |
|-------|-------|------|--------|-----|-----|-----|-----|
| **Normal** | 155 | 0.245 | 0.254 | 0.195 | 0.292 | 0.057 | 0.398 |
| **Abnormal** | 321 | 0.430 | 0.270 | 0.226 | 0.324 | 0.111 | 3.187 |

**분석**:
- ✅ **평균 차이**: 1.76배 (acc_Y보다 약함)
- ⚠️ **중앙값 차이 작음**: 0.270 vs 0.254 (6% 차이)
- 🔴 **이상치**: 불량 최대 3.187 (정상 최대 0.398의 8배)

#### Gyro_Y_rms (회전 불안정)

| Class | Count | Mean | Median | Q25 | Q75 | Min | Max |
|-------|-------|------|--------|-----|-----|-----|-----|
| **Normal** | 155 | 0.722 | 0.701 | 0.573 | 0.867 | 0.161 | 1.200 |
| **Abnormal** | 321 | 1.003 | 0.732 | 0.593 | 0.867 | 0.358 | 4.570 |

**분석**:
- ✅ **평균 차이**: 1.39배
- ⚠️ **Q25/Q75 중첩**: 불량 Q75 = 정상 Q75 (0.867)
- 🔴 **이상치**: 불량 최대 4.570 (정상 최대 1.200의 3.8배)

---

## 3️⃣ RMS Features 상관관계 분석

### 3.1 고상관 쌍 (|r| > 0.95)

| Rank | Feature 1 | Feature 2 | Correlation | 해석 |
|------|-----------|-----------|-------------|------|
| 1 | **acc_Y_rms** | **acc_Sum_rms** | **0.9943** | Y축이 합성 진동 지배 |
| 2 | **acc_Z_rms** | **acc_Sum_rms** | **0.9849** | Z축도 합성에 큰 기여 |
| 3 | **acc_Y_rms** | **acc_Z_rms** | **0.9647** | Y-Z축 강한 연동 |
| 4 | **acc_X_rms** | **acc_Z_rms** | **0.9538** | X-Z축 연동 |
| 5 | **acc_X_rms** | **acc_Sum_rms** | **0.9520** | X축도 합성에 기여 |

### 3.2 중복성 문제 및 해결 방안

**문제점**:
- acc_Y/Z/Sum_rms이 거의 동일한 정보 (r>0.96)
- **다중공선성** 위험: Linear model에서 계수 불안정
- **과적합** 위험: 중복 feature가 모델 복잡도만 증가

**해결 방안**:

#### Option A: 수동 Feature 선택 (권장)
```python
# 고상관 쌍 중 하나만 선택
selected_rms = [
    'acc_Y_rms',      # Y축 RMS (가장 강력, 2.22배)
    'acc_X_rms',      # X축 RMS (Y/Z와 상관 낮음)
    'Gyro_Y_rms',     # Y축 회전 (acc와 독립적)
    'Gyro_X_rms',     # X축 회전
    # acc_Sum_rms 제외 (acc_Y와 r=0.994)
    # acc_Z_rms 제외 (acc_Y와 r=0.965)
    # Gyro_Z_rms 제외 (역방향 패턴)
]
```

#### Option B: XGBoost Feature Importance 기반 선택
- XGBoost 학습 후 importance 확인
- 중복 쌍 중 importance 낮은 것 제거
- **장점**: 데이터 기반 객관적 선택
- **단점**: 해석 가능성 감소

#### Option C: PCA 차원 축소
- 7개 RMS → 3-4개 주성분
- **장점**: 정보 손실 최소화
- **단점**: 물리적 해석 어려움

**권장**: **Option A (수동 선택) → Option B (검증)** 순서

---

## 4️⃣ 시각화 분석 결과

### 4.1 2D Scatter Plots

#### acc_Sum_rms vs acc_Sum_kurtosis
- **X축**: acc_Sum_rms (진동 크기)
- **Y축**: acc_Sum_kurtosis (충격성)
- **패턴**:
  - 정상: 저RMS, 저Kurtosis 영역에 집중
  - 불량: RMS 높은 영역으로 확산, Kurtosis 다양
  - **분리**: RMS > 0.4에서 대부분 불량

#### acc_Y_rms vs acc_Y_kurtosis
- **패턴**: acc_Sum과 유사
- **특징**: 불량품 중 일부가 **고RMS + 고Kurtosis** 클러스터
- **해석**: 충격성 + 진동 크기가 함께 높은 불량 유형 존재

#### Gyro_Y_rms vs Gyro_Y_kurtosis
- **패턴**: acc보다 분리 약함
- **특징**: 회전 불안정은 충격성과 독립적

#### acc_Y_std vs acc_Y_rms
- **패턴**: **완벽한 선형 관계** (r≈1.0)
- **해석**: std와 rms는 거의 동일 정보
- **조치**: 둘 중 하나만 사용 (RMS 선호)

### 4.2 Boxplots

**모든 RMS features의 공통 패턴**:
- 📊 **불량품 분포가 정상품보다 넓음** (표준편차 큼)
- 🔴 **불량품에 극단적 이상치** 존재
- ⚠️ **중앙값 중첩** 문제 (단순 임계값으로 분리 어려움)
- ✅ **평균 차이는 명확** (1.4~2.2배)

**예외**:
- **Gyro_Z_rms**: 불량품이 오히려 낮은 평균

---

## 5️⃣ 주파수 대역별 RMS (추가 필요)

### 5.1 현재 부족한 Feature

**문서에서 요구하는 대역 RMS**:
- **1-10 Hz**: 저주파 진동 (언밸런스, 정렬 불량)
- **10-50 Hz**: 중주파 진동 (1×/2× 회전 성분)
- **50-150 Hz**: 고주파 진동 (베어링, 기어, 충격성)

**현재 상태**:
- ✅ 전체 대역 RMS는 추출됨 (acc_X/Y/Z/Sum_rms, Gyro_X/Y/Z_rms)
- ❌ 주파수 대역별 RMS는 **미추출**

### 5.2 대역별 RMS 추가 필요성

**문서 참조**:
> "하이브리드 룰: (2×/1×>T1) OR **(rms_50_150>T2)** OR (CF>T3)"

**장점**:
1. **원인 진단**: 어느 주파수 대역이 문제인지 파악
2. **Feature 차별화**: 전체 RMS와 다른 정보 제공
3. **하이브리드 룰**: 모델과 독립적인 안전망

**추출 방법**:
```python
# Bandpass filter + RMS 계산
def band_rms(signal, fs, low, high):
    filtered = bandpass_filter(signal, low, high, fs)
    return np.sqrt(np.mean(filtered**2))

# 3개 대역
rms_1_10 = band_rms(signal, fs=512, low=1, high=10)
rms_10_50 = band_rms(signal, fs=512, low=10, high=50)
rms_50_150 = band_rms(signal, fs=512, low=50, high=150)
```

---

## 6️⃣ RMS Feature 선택 권장사항

### 6.1 Phase 2 XGBoost Baseline

**추천 Feature Set (4개)**:
```python
rms_features_baseline = [
    'acc_Y_rms',      # 가장 강력 (2.22배), Y축 진동
    'acc_X_rms',      # X축 진동 (Y/Z와 독립적)
    'Gyro_Y_rms',     # Y축 회전 (1.39배)
    'Gyro_X_rms',     # X축 회전 (1.11배)
]
# 제외: acc_Sum_rms (acc_Y와 r=0.994)
# 제외: acc_Z_rms (acc_Y와 r=0.965)
# 제외: Gyro_Z_rms (역방향 패턴)
```

**근거**:
- 중복 제거로 **다중공선성 해소**
- 물리적으로 **독립적인 측정값**
- **해석 가능성** 유지

### 6.2 Phase 3 Feature Engineering

**추가 Feature 후보**:
1. **대역별 RMS** (우선순위 높음)
   ```python
   'rms_1_10_Hz',
   'rms_10_50_Hz',
   'rms_50_150_Hz',
   ```

2. **RMS 비율**
   ```python
   'rms_Y_to_X_ratio',      # Y/X 비율 (방향성)
   'rms_high_to_low_ratio', # 고주파/저주파 비율 (충격성)
   ```

3. **RMS 변화율** (윈도우 간)
   ```python
   'rms_delta',             # 시간에 따른 RMS 변화
   'rms_trend',             # 증가/감소 추세
   ```

---

## 7️⃣ 결론 및 Action Items

### ✅ 핵심 발견

1. **RMS는 불량 감지의 핵심**
   - acc_Y_rms: 2.22배 차이 (가장 강력)
   - 모든 acc RMS: 1.5~2.2배 차이

2. **극단적 이상치가 핵심**
   - 불량품 대부분은 정상과 유사
   - 일부 극단값이 평균을 올림
   - **이상치 탐지** 접근 유효성 시사

3. **Feature 중복성 높음**
   - 5개 RMS 쌍이 r>0.95
   - Feature 선택 필수

4. **대역별 RMS 부재**
   - 문서에서 핵심 요구사항
   - Phase 3에서 추가 필요

### 📋 Immediate Action Items

**Phase 2 (현재)**:
- [ ] RMS feature 4개로 축소 (acc_Y, acc_X, Gyro_Y, Gyro_X)
- [ ] XGBoost baseline 학습
- [ ] Feature importance로 선택 검증

**Phase 3 (Feature Engineering)**:
- [ ] 대역별 RMS 추출 (1-10, 10-50, 50-150 Hz)
- [ ] RMS 비율 features 생성
- [ ] 하이브리드 룰 구현 (rms_50_150 > T2)

**Phase 4 (Advanced)**:
- [ ] Autoencoder anomaly detection (이상치 특성 활용)
- [ ] 대역별 에너지 시각화
- [ ] 원인 진단 로직 (어느 대역이 문제인지)

---

## 8️⃣ 시각화 파일 목록

### 생성된 파일 (claudedocs/eda_results/)

**분석 데이터 (AI 읽기 가능)**:
1. `rms_statistics_by_class.csv` - 클래스별 상세 통계
2. `rms_abnormal_to_normal_ratios.csv` - 불량/정상 비율
3. `rms_correlation_matrix.csv` - 상관계수 행렬
4. `rms_high_correlation_pairs.csv` - 고상관 쌍
5. `visualization_summary.csv` - 종합 요약

**시각화 + 데이터 (visualizations/)**:
- 6개 scatter plots (.png + .csv)
- 7개 boxplots (.png + .csv + summary.csv)

**총 26개 파일** (이미지 13개 + 데이터 13개)

모든 시각화의 원본 데이터가 CSV로 저장되어 **AI가 수치를 직접 분석 가능**합니다.
